module.exports = class FormElements {
    constructor(serviceKey, elements, step, stepCount) {

        this.service_key = serviceKey;
        this.elements = elements;
        this.step = step;
        this.step_count = stepCount;
    }
} 